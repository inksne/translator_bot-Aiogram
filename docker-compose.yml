version: '3.8'

services:
  back:
    image: back-image:latest
    container_name: back-container
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "10000:10000"
    command: ["python", "main.py"]